# Compute mean, median, and standard deviation of a random array.
import numpy as np
arr=np.random.rand(10)
meanx=np.mean(arr)
medianx=np.median(arr)
stdx=np.median(arr)
print(arr)
print(meanx)
print(medianx)
print(stdx)
